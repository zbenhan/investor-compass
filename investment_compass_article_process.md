# 投资罗盘网站添加文章标准化流程

## 1. 准备阶段
| 步骤 | 操作 | 说明 |
|------|------|------|
| 1.1 | 确定文章主题与分类 | 根据网站现有分类（价值投资、技术分析、宏观投资、叙事投资、综合比较）确定文章所属类别 |
| 1.2 | 规划语言版本 | 确定需要创建的语言版本（中文、英文、法文、西班牙文） |
| 1.3 | 准备文章内容 | 编写文章正文、标题、摘要、关键词等内容，确保内容质量和准确性 |

## 2. 文章创建阶段
| 步骤 | 操作 | 说明 |
|------|------|------|
| 2.1 | 创建中文版本文章 | <ul><li>复制现有文章模板或创建新的HTML文件</li><li>文件名使用英文小写、短横线分隔（如：graham-vs-growth.html）</li><li>保存到 `articles/zh/` 目录</li><li>设置正确的HTML结构和Bootstrap样式</li><li>添加SEO meta标签（标题、描述、关键词）</li><li>添加语言切换器和导航栏</li></ul> |
| 2.2 | 创建英文版本文章 | <ul><li>基于中文版本翻译内容</li><li>保存到 `articles/en/` 目录</li><li>确保HTML结构与中文版本一致</li><li>更新meta标签为英文内容</li><li>设置正确的语言属性 `lang="en"`</li></ul> |
| 2.3 | 创建法文版本文章 | <ul><li>基于中文版本翻译内容</li><li>保存到 `articles/fr/` 目录</li><li>确保HTML结构与中文版本一致</li><li>更新meta标签为法文内容</li><li>设置正确的语言属性 `lang="fr"`</li></ul> |
| 2.4 | 创建西班牙文版本文章 | <ul><li>基于中文版本翻译内容</li><li>保存到 `articles/es/` 目录</li><li>确保HTML结构与中文版本一致</li><li>更新meta标签为西班牙文内容</li><li>设置正确的语言属性 `lang="es"`</li></ul> |

## 3. 导航与链接更新
| 步骤 | 操作 | 说明 |
|------|------|------|
| 3.1 | 更新articles.html导航 | <ul><li>打开 `articles.html` 文件</li><li>在对应的分类（基础文章/最新文章）下添加文章链接</li><li>确保链接格式为：`articles/zh/文章文件名.html`</li><li>链接文本使用文章中文标题</li></ul> |
| 3.2 | 更新文章链接配置 | <ul><li>在 `articles.html` 文件中的 `articleLinks` 对象中添加新文章的所有语言版本标题</li><li>确保包含 `zh`、`en`、`fr`、`es` 四个语言的标题</li></ul> |

## 4. Sitemap更新
| 步骤 | 操作 | 说明 |
|------|------|------|
| 4.1 | 打开sitemap.xml | 定位到项目根目录下的 `sitemap.xml` 文件 |
| 4.2 | 添加中文文章链接 | <ul><li>在 `<urlset>` 标签内添加新的 `<url>` 条目</li><li>`<loc>` 标签内容为：`https://www.example.com/articles/zh/文章文件名.html`</li><li>`<lastmod>` 标签内容为当前日期（YYYY-MM-DD格式）</li><li>`<changefreq>` 标签设置为 `monthly`</li><li>`<priority>` 标签设置为 `0.8`</li></ul> |
| 4.3 | 添加英文文章链接 | 重复4.2步骤，将URL中的 `zh` 替换为 `en` |
| 4.4 | 添加法文文章链接 | 重复4.2步骤，将URL中的 `zh` 替换为 `fr` |
| 4.5 | 添加西班牙文文章链接 | 重复4.2步骤，将URL中的 `zh` 替换为 `es` |

## 5. 测试与验证
| 步骤 | 操作 | 说明 |
|------|------|------|
| 5.1 | 本地服务器测试 | <ul><li>确保本地服务器已启动（`npm run start`）</li><li>访问 `http://localhost:8000` 验证网站正常运行</li></ul> |
| 5.2 | 文章访问测试 | <ul><li>直接访问所有语言版本的文章URL，确保返回200状态码</li><li>检查文章内容显示是否正常</li><li>测试语言切换功能是否正常工作</li></ul> |
| 5.3 | 导航链接测试 | <ul><li>通过 `articles.html` 页面点击新添加的文章链接，确保能正确跳转到对应文章</li><li>测试不同语言版本下的导航链接是否正确</li></ul> |
| 5.4 | Sitemap验证 | <ul><li>使用在线Sitemap验证工具检查XML格式是否正确</li><li>确保所有新添加的文章链接都已包含在Sitemap中</li></ul> |

## 6. 发布与维护
| 步骤 | 操作 | 说明 |
|------|------|------|
| 6.1 | 提交代码更新 | 将所有修改提交到版本控制系统 |
| 6.2 | 定期检查与更新 | <ul><li>定期检查文章内容的时效性</li><li>根据需要更新文章内容或SEO标签</li><li>更新Sitemap中的 `<lastmod>` 日期</li></ul> |

## 7. 最佳实践
- 保持所有语言版本的文章结构一致
- 使用清晰、描述性的文件名
- 为每篇文章添加合适的SEO标签
- 确保文章内容符合网站的整体风格和质量标准
- 定期备份文章内容
- 遵循网站的命名规范和目录结构

## 8. 资源参考
- 现有文章模板：可参考 `articles/zh/graham-vs-growth.html`
- 网站导航：`articles.html`
- Sitemap文件：`sitemap.xml`
- 语言配置：`js/main.js` 和 `data/languages/` 目录

## 9. 常见问题与解决方案
| 问题 | 解决方案 |
|------|----------|
| 语言切换不工作 | 检查 `articleLinks` 对象是否正确配置了所有语言版本的标题 |
| 文章无法访问 | 检查文件路径、文件名是否正确，确保文件已上传到正确目录 |
| SEO标签不显示 | 检查meta标签的属性是否正确设置，确保语言文件中包含对应的翻译 |

## 10. 相关文件列表
- `articles.html` - 文章导航页面
- `sitemap.xml` - 网站地图
- `js/main.js` - 主要JavaScript文件
- `data/languages/` - 语言配置文件目录
- `articles/zh/` - 中文文章目录
- `articles/en/` - 英文文章目录
- `articles/fr/` - 法文文章目录
- `articles/es/` - 西班牙文文章目录

## 11. 版本历史
| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-01-03 | 初始版本 |
